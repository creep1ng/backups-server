version: 1
storage_box:
  host: example.com
  user: backup
  ssh_key_path: tests/fixtures/dummy_key
  repo_path: /backups
services:
- name: web
  compose_file: tests/fixtures/docker-compose.yml: |
    this is not valid: [

